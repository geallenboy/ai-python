ai agent

```
1.设置你的虚拟环境

python -m venv .venv
source .venv/bin/activate

2.安装依赖项
pip install --no-cache-dir -r requirements.txt

3.导出您的 OpenAI 密钥

export OPENAI_API_KEY=sk-***

4.运行

python xx.py
```